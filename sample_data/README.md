Place sample PDF/DOCX/TXT files in this folder for local testing.

Example:
- sample_data/employee_handbook.pdf

The notebook originally used `/content/data` in Colab — adapt `DATA_FOLDER` accordingly.
